require 'spec_helper'

describe JellyfishLdapauth do
  it 'logs an LDAP user in if the credentials are valid' do
  end

  it 'does not log an LDAP user in if the credentials are invalid' do
  end
end
